# ── CryptoMinds 环境变量配置 ──────────────────────────
# 复制本文件为 .env 并按实际情况修改
#   cp .env.example .env

# ── BSC RPC 节点（可选，有默认值）──
# BSC_RPC=https://bsc-dataseed1.binance.org/

# ── 演示模式（可选）──
# 设为 true 时跳过真实链上交易，输出模拟结果
# DEMO_MODE=false

# ── 调试模式（可选）──
# DEBUG=false
